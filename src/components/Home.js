import './Home.css';

function Home() {
  const home = 'Life is a math education, in order to gain the most, you have to convert the negatives into positives';

  return (
    <section>
      <h3> Welcome to the Best Experience!</h3>
      <p>{home}</p>
    </section>
  );
}

export default Home;
